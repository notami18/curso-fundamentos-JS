let base = 5;
let altura = 7;

/*function trianguleArea(base, altura) {
  return base * altura / 2
}*/

let trianguleArea = (base, altura) => base * altura / 2

console.log(`El area de un triangulo de altura ${base} y base ${altura} es: ${trianguleArea(base, altura)}`);
